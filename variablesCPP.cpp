// Example program
#include <iostream>
#include <string>
#include <windows.h>
using namespace std;

int main() {
    int a = 5, b = 4; // declaring and initializing integer variables
    float c = 3.5, d = 2.4; // float type variable declaration and initialization
    bool x = true ; // boolean type of variable can have only true or false
    string name = "Beula" ; // strings are sequence of characters within double quotes
    char y = 'C' ; // character type variables can store only one character
    system ("Color BA"); // to display colored text(A) in a background color (B) specified, 
    cout << " The sum of a and b is " << a + b << endl;
    cout << " The difference between c and d is " << c - d << endl;
    cout << " the boolean value of x is " << x << endl;
    cout << " My name is " << name << endl;
    
}
